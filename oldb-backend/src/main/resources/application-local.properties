# Local development — schema: oldb_local
spring.config.activate.on-profile=local

security.jwt.secret-key=${JWT_SECRET:3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b}

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:mydatabase}?currentSchema=oldb_local
spring.datasource.username=${DB_USER:myuser}
spring.datasource.password=${DB_PASSWORD:mypassword}
spring.jpa.properties.hibernate.default_schema=oldb_local

# Katalog editörü ile manuel giriş yapılacağı için otomatik import kapalı
catalog.openlibrary.import-on-empty=false

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:huseyinavnikadioglu@gmail.com}
spring.mail.password=${MAIL_PASSWORD:upiqpwzorylplwvl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

management.metrics.export.dynatrace.enabled=false
